# Notes

Drop your PDF files here, then reference them from `js/catalog.js` via the
`file` field, e.g. `file: "notes/mbbs-y1-anatomy.pdf"`.

Keep filenames lowercase with hyphens — they become part of the delivery link.
